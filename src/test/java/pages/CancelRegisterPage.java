package pages;

import org.openqa.selenium.WebDriver;
import utils.SeleniumBase;

public class CancelRegisterPage extends SeleniumBase {

    public CancelRegisterPage(WebDriver driver) {
        super(driver);
    }
}
